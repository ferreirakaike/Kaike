using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class HealthSign : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void FixedUpdate()
    {
        // find a way to make this fly straight up
        //this.gameObject.transform.position = transform.up * 3.0f;
    }
}
